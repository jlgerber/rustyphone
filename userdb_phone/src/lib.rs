/// The `opt` submodule houses all of the structopt structures
/// defining the cli interface.
pub mod opt;
/// The process submodule houses all of the async functions which
/// do the actual crud work.
pub mod process;